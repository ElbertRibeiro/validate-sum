name: Publish package to the Maven Central Repository

on:
  workflow_call:

jobs:
  publish:
    name: Publish in Sonatype
    runs-on: ubuntu-latest
    steps:
      - name: Checkout project sources
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '8.0.412+8'
          distribution: 'temurin'
          server-id: ossrh
          server-username: 1YJpzfRB
          server-password: FhAUgf+IbrfcEBOqwB92mCe5idcVDiAHda+YPMQfN+q3
          gpg-private-key: ${{ secrets.GPG_SIGNING_KEY }}
          gpg-passphrase: ${{ secrets.GPG_SIGNING_KEY_PASSWORD }}

      - name: Set Version Artefact
        run: mvn versions:set -DnewVersion=${{ github.event.release.tag_name }}

      - name: Deploy in Sonatype
        run: mvn -P release deploy -DskipTests
        env:
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPG_SIGNING_KEY_PASSWORD }}